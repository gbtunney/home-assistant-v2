# ============================================================
# DEVICE — ENV TEMP 1 (DEPLOYED)
# ============================================================
#
# This is a DEPLOYED device file.
# It lives at the root of the ESPHome config directory so it
# appears in the ESPHome add-on UI.
#
# Role:
# - Environmental temperature monitoring
# - 3 Dallas probe pairs
# - Bluetooth Proxy infrastructure
# - Standard diagnostics
#
# ============================================================

substitutions:
  log_level: VERY_VERBOSE
  #wifi_ssid: "Snailicide"
  #wifi_password: "Betta$11"
  #api_encryption_key: "UK183nZ61Ngi/7py/HnBtsMQURS8lYXnBke8iBepuFo="
  #ota_password: "9261fadd458c420a7f03c4467a4be901"
  # device_use_address: "192.168.1.14"
  device_use_address: "192.168.1.25"
  ble_proxy: true
  # ----------------------------------------------------------
  # DEVICE IDENTITY
  # ----------------------------------------------------------
  device_id: gbt_test_proxy
  device_hostname: "gbt-test-proxy"
  device_friendly_name: "GBT Test Proxy"
  device_description: "I repeat , just a test"

packages:
  # ----------------------------------------------------------
  # BASE + FLEET
  # ----------------------------------------------------------
  base: !include common/universal_base.yaml
  board: !include common/platforms/esp32doit.yaml
  fleet: !include fleets/env_sensors.yaml

  # ----------------------------------------------------------
  # CAPABILITIES
  # ----------------------------------------------------------
  diagnostics: !include common/capabilities/diagnostics.yaml
  #ble: !include common/capabilities/bluetooth_proxy.yaml

captive_portal:

one_wire:
  - platform: gpio
    pin: 22
    id: dallas

sensor:
  - platform: dallas_temp
    address: 0xac00000034ba3528 #
    name: "Test Dallas Temperature 1"
